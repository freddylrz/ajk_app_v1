/**
 * ============================================================
 * PAGE: Klaim — Detail
 * API  : belum ada endpoint detail klaim di sisi server, jadi
 *        halaman dibiarkan kosong (tanda "-") sampai endpoint
 *        tersedia (tidak diisi data bikinan).
 * ============================================================
 */

$(function () {
    // TODO: sambungkan ke endpoint detail klaim begitu tersedia.
});
