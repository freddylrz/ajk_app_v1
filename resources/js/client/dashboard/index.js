/**
 * ============================================================
 * PAGE: Dashboard Client
 * API  : belum ada endpoint ringkasan dashboard client — kartu
 *        statistik & tabel klaim terbaru dibiarkan kosong sampai
 *        endpoint-nya tersedia (tidak diisi data bikinan).
 * ============================================================
 */

$(function () {
    // TODO: sambungkan ke endpoint ringkasan dashboard client bila sudah tersedia.
});
